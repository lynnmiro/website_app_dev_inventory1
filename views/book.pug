extends layout.pug

block content
  h1.mt-5 #{book.title} 
    a.btn.btn-primary(href='./' + book.id + '/update') Edit
  p Title: #{book.title}
  p Author: #{book.author}
  p Page Numbers: #{book.numPages}